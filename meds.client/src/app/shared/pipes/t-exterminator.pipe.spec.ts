import { TExterminatorPipe } from './t-exterminator.pipe';

describe('TExterminatorPipe', () => {
  it('create an instance', () => {
    const pipe = new TExterminatorPipe();
    expect(pipe).toBeTruthy();
  });
});
