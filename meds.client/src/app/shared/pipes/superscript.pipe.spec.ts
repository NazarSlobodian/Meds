import { SuperscriptPipe } from './superscript.pipe';

describe('SuperscriptPipe', () => {
  it('create an instance', () => {
    const pipe = new SuperscriptPipe();
    expect(pipe).toBeTruthy();
  });
});
