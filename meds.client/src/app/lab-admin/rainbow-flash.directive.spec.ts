import { RainbowFlashDirective } from './rainbow-flash.directive';

describe('RainbowFlashDirective', () => {
  it('should create an instance', () => {
    const directive = new RainbowFlashDirective();
    expect(directive).toBeTruthy();
  });
});
